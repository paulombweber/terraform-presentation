{
  "name": "somefunction",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rimraf build",
    "build:compile": "tsc index.ts",
    "build": "rimraf dist && tsc -p tsconfig.build.json && npm prune --production"
  },
  "dependencies": {
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "typescript": "^3.3.3"
  }
}
